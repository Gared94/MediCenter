@model IEnumerable<MediCenter3.Models.PROVEEDORES>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
    @Html.ActionLink("Nuevo proveedor", "Create", "PROVEEDORES", new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PROVEEDOR)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DIRECCION)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TELEFONO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOTAS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PROVEEDOR)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DIRECCION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TELEFONO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NOTAS)
        </td>
        <td>
            <button type="button" class="btn btn-primary" onclick="location.href='@Url.Action("Edit","PROVEEDORES", new { id = item.ID_PROVEEDOR  })'">Editar</button>
            @*@Html.ActionLink("Edit", "Edit", new { id = item.ID_PROVEEDOR }) |*@
            <button type="button" class="btn btn-primary" onclick="location.href='@Url.Action("Details","PROVEEDORES", new {  id = item.ID_PROVEEDOR })'">Detalles</button>
            @*@Html.ActionLink("Details", "Details", new { id = item.ID_PROVEEDOR }) |*@
            <button type="button" class="btn btn-primary" onclick="location.href='@Url.Action("Delete","PROVEEDORES", new { id = item.ID_PROVEEDOR  })'">Borrar</button>
            @*@Html.ActionLink("Delete", "Delete", new { id = item.ID_PROVEEDOR })*@
        </td>
    </tr>
}

</table>
