@model IEnumerable<MediCenter3.Models.SUCURSALES>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@

    @Html.ActionLink("Nueva sucursal", "Create", "SUCURSAL", new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SUCURSAL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DIRECCION)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TELEFONO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SUCURSAL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DIRECCION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TELEFONO)
        </td>
        <td>
            <button type="button" class = "btn btn-primary" onclick="location.href='@Url.Action("Edit","SUCURSALES", new { id = item.ID_SUCURSAL })'">Editar</button>
            @*@Html.ActionLink("Edit", "Edit", new { id = item.ID_SUCURSAL }) |*@
            <button type="button" class = "btn btn-primary" onclick="location.href='@Url.Action("Details","SUCURSALES", new { id = item.ID_SUCURSAL })'">Detalles</button>
            @*@Html.ActionLink("Details", "Details", new { id = item.ID_SUCURSAL }) |*@
            <button type="button" class = "btn btn-primary" onclick="location.href='@Url.Action("Delete","SUCURSALES", new { id = item.ID_SUCURSAL })'">Borrar</button>
            @*@Html.ActionLink("Delete", "Delete", new { id = item.ID_SUCURSAL })*@
        </td>
    </tr>
}

</table>
